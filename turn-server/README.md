Acest director conține un server TURN (coturn) pentru WebRTC.

Configurația Docker a fost mutată în fișierul `docker-compose.yml` de la rădăcina proiectului.
Vechiul fișier `docker-compose.yml` de aici poate fi șters.
